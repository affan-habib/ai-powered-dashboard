import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Analytics Dashboard - Intelligent Data Visualization",
  description: "AI-powered dashboard that generates interactive visualizations from natural language prompts. Create charts, graphs, and insights with the power of artificial intelligence.",
  keywords: ["AI", "dashboard", "analytics", "data visualization", "artificial intelligence", "charts"],
  authors: [{ name: "Affan Habib" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
